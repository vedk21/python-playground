# Array package

from array import array

arr = array('i', [1, 2, 3, 4, 5])
print(arr[3]) # 4

# In python, lists are dynamic type data-structure, but when we need fixed size linear array data structure we use array package.
